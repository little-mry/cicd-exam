import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import Navigation from "../../components/Navigation/Navigation";

describe("Navigation component", () => {
  describe("US5: As a user I want to be able to navigate between the booking view and the confirmation view ", () => {
    it("should show message 'no booking' if user navigate to confiamtion view, and session storage is empty (AC2)", async () => {});
    it("should bookning if user navigate to confiamtion view, and a booking exist in session storage (AC2)", async () => {});
  });
});
